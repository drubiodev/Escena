/**
 * @typedef {Object} BlockDefinition
 * @property {string} type Stable key stored in block instances.
 * @property {string} label Human-readable palette label.
 * @property {string} category Palette grouping label.
 * @property {string} path Absolute component URL.
 * @property {string} icon Icon URL or identifier.
 * @property {{w: number, h: number}} size Default logical dimensions.
 * @property {string} [editable] Prop key used for inline editing.
 * @property {Object[]} props Inspector schema and instance defaults.
 */

export { };
